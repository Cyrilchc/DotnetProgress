<Window x:Class="WpfProgress.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfProgress"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="MainWindow_OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30px"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <ProgressBar Name="ProgressBar" Height="20px"/>
            <TextBlock Name="ProgressName" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </StackPanel>
        <Button Grid.Row="1" HorizontalAlignment="Right" Margin="0 0 5 5" Click="MainWindow_OnLoaded">
            <TextBlock>Redémarrer</TextBlock>
        </Button>
    </Grid>
</Window>